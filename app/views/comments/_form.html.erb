<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :name %>: </br>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :Comment %>: </br>
		<%= f.text_area :body %>
	</p>

	<p>
		<%= f.submit "Post" %>
	</p>
<% end %>